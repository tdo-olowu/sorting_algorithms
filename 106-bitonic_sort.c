#include <sort.h>

/**
 * bitonic_sort - sorts an array using Bitonic sort
 * @array: the number of items in the array
 * @size: the array to be sorted
 * Return: none
 */
void bitonic_sort(int *array, size_t size)
{
	return;
}
