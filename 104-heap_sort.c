#include <sort.h>

/**
 * heap_sort - sorts an array using the Heap sort technique
 * @array: the array to be sorted
 * @size: the size of the array to be sorted
 * Return: none.
 */
void heap_sort(int *array, size_t size)
{
	return;
}
