#include <sort.h>

/**
 * shell_sort - sorts an array using Shell Sort
 * uses Knuth sequence as gap sequence
 * @array: array to be arraynged
 * @size: the number of items in the array
 * Return: none
 */
void shell_sort(int *array, size_t size)
{
	return;
}
