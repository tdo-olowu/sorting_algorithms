#include <deck.h>


/**
 * sort_deck - sorts a deck of cards
 * (the virtual representation of cards, anyway)
 * @deck: pointer to head of a deck_node_t struct
 * Return: none.
 */
void sort_deck(deck_node_t **deck)
{
	return;
}
