#include <sort.h>

/**
 * merge_sort - sorts an array using the merge sort technique
 * uses the top-down merge sort algorithm
 *
 * @array: the array to be sorted
 * @size: the size of the array in question
 * Return: none.
 */
void merge_sort(int *array, size_t size)
{
	return;
}
